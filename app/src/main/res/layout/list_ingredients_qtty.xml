<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="recipeIngredientsQtty"
            type="app.kitsu.mercerecetas.database.RecipeIngredientQuantity" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/ingredient_name"
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:textAlignment="textStart"
            android:gravity="start"
            android:text="@{recipeIngredientsQtty.ingredientName}"
            android:textColor="#de000000"
            android:textSize="20sp"
            tools:text="Patata"
            />

        <TextView
            android:id="@+id/ingredient_qtty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:layout_marginTop="16dp"
            android:gravity="end"
            android:textAlignment="textEnd"

            android:textColor="#de000000"
            android:textSize="20sp"
            app:ingredientQttyFormatted="@{recipeIngredientsQtty.ingredientQtty}"
            tools:text="6 u." />
    </LinearLayout>
</layout>